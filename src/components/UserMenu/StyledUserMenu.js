import styled from 'styled-components';

export const StyledUserMenu = styled.div`
    position: absolute;
    display: flex;
    p {
        color:white;
    }
    button { 
        margin-left: 20px;
    }
`